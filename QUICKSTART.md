# Быстрый старт с аутентификацией

## Что было добавлено

✅ **Полная система аутентификации** на основе HTTP-only cookies
✅ **Pinia store** для управления состоянием пользователя
✅ **Защита маршрутов** через Vue Router guards
✅ **Красивые формы** входа и регистрации
✅ **Автоматический редирект** при 401 ошибке
✅ **Поддержка ролей** (USER, ADMIN, MANAGER)

## Структура файлов

```
src/
├── services/
│   ├── authService.js      ✅ Обновлен (login, register, logout, getCurrentUser)
│   └── api.js              ✅ Обновлен (interceptor для 401)
├── store/
│   ├── index.js            ✅ Существующий (Pinia)
│   └── auth.js             ✨ НОВЫЙ (Auth store)
├── router/
│   └── index.js            ✅ Обновлен (navigation guards)
├── views/
│   └── AuthView.vue        ✅ Обновлен (формы входа/регистрации)
└── components/
    └── layout/
        └── Header.vue      ✅ Обновлен (использует auth store)
```

## Как использовать

### 1. Вход/Регистрация
Пользователь переходит на `/auth` и видит формы входа и регистрации с вкладками.

### 2. Защищенные маршруты
Страницы с `meta: { requiresAuth: true }` автоматически защищены:
- `/profile` - профиль пользователя
- `/contracts` - договоры
- `/contract/new` - создание договора

### 3. Использование в компонентах

```vue
<script setup>
import { useAuthStore } from '@/store/auth'

const authStore = useAuthStore()

// Проверить авторизацию
if (authStore.isAuthenticated) {
  console.log('Пользователь:', authStore.username)
}

// Проверить роль
if (authStore.isAdmin) {
  console.log('Это администратор')
}
</script>
```

## Запуск приложения

```bash
# Установка зависимостей (если еще не установлены)
npm install

# Запуск dev-сервера
npm run dev
```

## Проверка работы

1. Откройте `http://localhost:5173`
2. Перейдите на `/auth`
3. Попробуйте:
   - ✨ Зарегистрироваться
   - ✨ Войти в систему
   - ✨ Перейти на защищенную страницу `/profile`
   - ✨ Выйти из системы

## Настройка бэкенда

Убедитесь, что ваш Spring Boot бэкенд:
- ✅ Работает на `http://localhost:8082`
- ✅ Имеет настроенный CORS для `http://localhost:5173`
- ✅ Возвращает cookies с правильными настройками

### Пример CORS конфигурации (Java):

```java
@Configuration
public class CorsConfig {
    @Bean
    public WebMvcConfigurer corsConfigurer() {
        return new WebMvcConfigurer() {
            @Override
            public void addCorsMappings(CorsRegistry registry) {
                registry.addMapping("/api/**")
                    .allowedOrigins("http://localhost:5173")
                    .allowedMethods("GET", "POST", "PUT", "DELETE", "PATCH")
                    .allowCredentials(true);
            }
        };
    }
}
```

## Основные эндпоинты

| Метод | Путь | Описание |
|-------|------|----------|
| POST | `/api/auth` | Вход (устанавливает cookie) |
| POST | `/api/registration` | Регистрация |
| POST | `/api/logout` | Выход (удаляет cookie) |
| GET | `/api/profile/me` | Данные текущего пользователя |

## Безопасность

- 🔒 **HTTP-only cookies** - защита от XSS
- 🔒 **SameSite=Lax** - защита от CSRF
- 🔒 **30 минут** время жизни токена
- 🔒 **Автоматический редирект** на /auth при истечении сессии

## Что дальше?

- [ ] Добавить "Запомнить меня"
- [ ] Добавить восстановление пароля
- [ ] Добавить двухфакторную аутентификацию
- [ ] Добавить валидацию форм
- [ ] Добавить loading состояния
- [ ] Добавить уведомления (toast)

## Troubleshooting

### Проблема: Cookie не отправляются
**Решение:** Проверьте `withCredentials: true` в `api.js` и CORS на бэкенде.

### Проблема: Бесконечный редирект
**Решение:** Убедитесь, что endpoint `/profile/me` доступен без ошибок 401 для авторизованных пользователей.

### Проблема: Ошибка CORS
**Решение:** Добавьте правильную CORS конфигурацию на бэкенде с `allowCredentials(true)`.

## Полная документация

Смотрите `AUTH_README.md` для подробной документации.

---

**Готово!** 🎉 Теперь ваше приложение имеет полноценную систему аутентификации!
