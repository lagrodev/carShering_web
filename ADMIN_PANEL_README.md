# Административная панель CarShare

## Обзор

Современная админ-панель для управления автопарком и моделями автомобилей.

## Доступ

**URL:** `/admin`

**Требования:** 
- Авторизация обязательна
- Роль: ADMIN или MANAGER

## Функциональность

### 1. Управление автомобилями

**Возможности:**
- ✅ Просмотр списка всех автомобилей с пагинацией
- ✅ Фильтрация по марке, модели, типу кузова
- ✅ Создание нового автомобиля
- ✅ Редактирование данных автомобиля (год, номер, цена)
- ✅ Изменение статуса (Available/Unavailable)
- ✅ Удаление автомобиля

**Поля создания:**
- Модель (выбор из существующих)
- Год выпуска
- Гос. номер
- VIN (уникальный)
- Стоимость аренды (₽/день)

### 2. Управление моделями

**Возможности:**
- ✅ Просмотр списка моделей с пагинацией
- ✅ Фильтрация по марке, типу кузова, классу
- ✅ Создание новой модели
- ✅ Редактирование модели
- ✅ Удаление модели

**Поля модели:**
- Марка (Brand)
- Модель (Model Name)
- Тип кузова (sedan, suv, hatchback, coupe, etc.)
- Класс (economy, comfort, business, premium)

### 3. Справочники

**Управление:**
- ✅ Марки автомобилей
- ✅ Названия моделей
- ✅ Классы автомобилей

**Возможности:**
- Просмотр всех элементов справочника
- Добавление новых элементов
- Используются для создания моделей

## API Endpoints

### Автомобили
```
GET    /api/admin/cars              - Список автомобилей
GET    /api/admin/cars/{id}         - Детали автомобиля
POST   /api/admin/cars              - Создать автомобиль
PATCH  /api/admin/cars/{id}         - Обновить автомобиль
PATCH  /api/admin/cars/{id}/state   - Изменить статус
DELETE /api/admin/cars/{id}         - Удалить автомобиль
```

### Модели
```
GET    /api/admin/models            - Список моделей
GET    /api/admin/models/{id}       - Детали модели
POST   /api/admin/models            - Создать модель
PATCH  /api/admin/models/{id}       - Обновить модель
DELETE /api/admin/models/{id}       - Удалить модель
```

### Справочники
```
POST   /api/admin/filters/brands    - Добавить марку
POST   /api/admin/filters/models    - Добавить название модели
POST   /api/admin/filters/classes   - Добавить класс
```

## Дизайн

**Стиль:** Минималистичный, современный, в едином стиле с остальным приложением

**Особенности:**
- 🎨 Градиентные кнопки (primary-600 → purple-600)
- 📱 Полностью адаптивный дизайн
- ⚡ Плавные анимации и переходы
- 🎯 Интуитивно понятный интерфейс
- ✨ Модальные окна с размытым фоном
- 🔔 Информативные состояния (загрузка, пустые списки)

## Цветовая схема

- **Основной цвет:** Primary (#6366F1)
- **Акцентный:** Purple (#9333EA)
- **Успех:** Green (#10B981)
- **Предупреждение:** Amber (#F59E0B)
- **Ошибка:** Red (#EF4444)
- **Нейтральный:** Gray (#6B7280)

## Компоненты

**Созданные файлы:**
- `views/AdminView.vue` - Основная страница с табами
- `components/admin/CarsManagement.vue` - Управление автомобилями
- `components/admin/CarFormModal.vue` - Форма создания/редактирования авто
- `components/admin/ModelsManagement.vue` - Управление моделями
- `components/admin/ModelFormModal.vue` - Форма создания/редактирования модели
- `components/admin/FiltersManagement.vue` - Управление справочниками

## Обработка ошибок

Все формы используют универсальный обработчик ошибок:
```javascript
import { getErrorMessage } from '../../utils/errorHandler'

catch (error) {
  alert(getErrorMessage(error, 'Ошибка операции'))
}
```

Автоматически извлекаются сообщения от бэкенда для любых статусов (400, 403, 404, 500).

## Валидация

**Клиентская:**
- Обязательные поля помечены `*`
- HTML5 валидация (required, min, max, type)
- Проверка уникальности VIN на бэкенде

**Серверная:**
- Все запросы валидируются через @Valid
- ValidationException возвращает понятные сообщения

## Права доступа

**Проверка в Header.vue:**
```vue
<router-link
  v-if="authStore.isAdmin"
  to="/admin"
  class="..."
>
  Админ панель
</router-link>
```

**Проверка в router:**
```javascript
if (to.meta.requiresAdmin && !authStore.isAdmin) {
  next({ name: 'Home' })
}
```

## Workflow

### Добавление автомобиля:
1. Создать модель (если нет)
2. Добавить марку/название в справочники (если нет)
3. Перейти на вкладку "Автомобили"
4. Нажать "Добавить автомобиль"
5. Заполнить форму
6. Сохранить

### Изменение статуса:
1. Найти автомобиль в списке
2. Нажать кнопку с иконкой "переключения"
3. Статус меняется мгновенно

### Редактирование:
1. Нажать кнопку "карандаш"
2. Изменить данные
3. Сохранить

## Пагинация

- 20 элементов на странице
- Навигация по страницам внизу списка
- Фильтры сохраняются при переходе между страницами

## Особенности реализации

✅ **Модульность** - каждая вкладка в отдельном компоненте  
✅ **Переиспользование** - общие модальные окна  
✅ **Оптимизация** - lazy loading для компонентов  
✅ **UX** - мгновенная обратная связь при действиях  
✅ **Безопасность** - проверка прав на клиенте и сервере  
✅ **Масштабируемость** - легко добавить новые вкладки  

---

**Готово к использованию!** 🎉
