# Полная документация админ-панели

## Обзор

Админ-панель реализует все эндпоинты из Swagger API документации и предоставляет полный функционал для управления системой каршеринга.

## Структура компонентов

### Основные компоненты

```
src/
├── views/
│   ├── AdminView.vue                      # Главная страница админ-панели с вкладками
│   ├── AdminCarCreateView.vue             # Создание нового автомобиля
│   ├── AdminCarDetailsView.vue            # Детали и редактирование автомобиля
│   ├── AdminUserDetailsView.vue           # Детали пользователя
│   └── AdminContractDetailsView.vue       # Детали контракта
├── components/
│   └── admin/
│       ├── CarsManagement.vue             # Управление автомобилями
│       ├── ModelsManagement.vue           # Управление моделями автомобилей
│       ├── ContractsManagement.vue        # Управление контрактами
│       ├── ClientsManagement.vue          # Управление клиентами
│       ├── DocumentsManagement.vue        # Управление документами
│       ├── FiltersManagement.vue          # Управление справочниками
│       ├── CarFormModal.vue               # Модальное окно формы автомобиля
│       └── ModelFormModal.vue             # Модальное окно формы модели
```

## Функциональность по вкладкам

### 1. Автомобили (Cars Management)

**API Endpoints:**
- `GET /api/admin/cars` - Получение списка автомобилей
- `POST /api/admin/cars` - Создание автомобиля
- `GET /api/admin/cars/{carId}` - Детали автомобиля
- `PATCH /api/admin/cars/{carId}` - Обновление автомобиля
- `DELETE /api/admin/cars/{carId}` - Удаление автомобиля
- `PATCH /api/admin/cars/{carId}/state` - Изменение статуса
- `GET /api/admin/cars/state` - Получение всех статусов

**Возможности:**
- ✅ Просмотр всех автомобилей с пагинацией
- ✅ Фильтрация по бренду, модели, году, типу кузова, классу и статусу
- ✅ Создание нового автомобиля
- ✅ Редактирование данных автомобиля
- ✅ Изменение статуса (AVAILABLE, UNAVAILABLE, IN_USE, MAINTENANCE, RESERVED, CLOSED)
- ✅ Удаление автомобиля
- ✅ Просмотр детальной информации

**Фильтры:**
- Бренд (мультивыбор с автодополнением)
- Модель (мультивыбор с автодополнением)
- Класс (мультивыбор с автодополнением)
- Тип кузова (dropdown)
- Статус (dropdown)
- Год от/до (числовые поля)

### 2. Модели (Models Management)

**API Endpoints:**
- `GET /api/admin/models` - Получение списка моделей
- `POST /api/admin/models` - Создание модели
- `GET /api/admin/models/{modelId}` - Детали модели
- `PUT /api/admin/models/{modelId}` - Обновление модели
- `DELETE /api/admin/models/{modelId}` - Удаление модели

**Возможности:**
- ✅ Просмотр всех моделей автомобилей
- ✅ Фильтрация по бренду, типу кузова и классу
- ✅ Создание новой модели
- ✅ Редактирование модели
- ✅ Удаление модели
- ✅ Пагинация

**Поля модели:**
- Бренд
- Название модели
- Тип кузова
- Класс автомобиля

### 3. Контракты (Contracts Management)

**API Endpoints:**
- `GET /api/admin/contracts` - Получение всех контрактов
- `GET /api/admin/contracts/{contractId}` - Детали контракта
- `PATCH /api/admin/contracts/{contractId}/confirm` - Подтверждение контракта
- `DELETE /api/admin/contracts/{contractId}/cancel` - Отмена контракта
- `PATCH /api/admin/contracts/contracts/{id}/confirm-cancellation` - Подтверждение отмены

**Возможности:**
- ✅ Просмотр всех контрактов с пагинацией
- ✅ Фильтрация по статусу, ID пользователя, ID автомобиля, бренду, типу кузова, классу
- ✅ Подтверждение контракта (статус PENDING → ACTIVE)
- ✅ Отмена контракта администратором
- ✅ Подтверждение запроса на отмену от пользователя
- ✅ Просмотр детальной информации о контракте

**Статусы контрактов:**
- ACTIVE - Активный
- PENDING - В ожидании подтверждения
- COMPLETED - Завершен
- CANCELLED - Отменен
- CANCELLATION_REQUESTED - Запрошена отмена пользователем

### 4. Клиенты (Clients Management)

**API Endpoints:**
- `GET /api/admin/users` - Получение списка пользователей
- `GET /api/admin/users/{userId}` - Детали пользователя
- `PATCH /api/admin/users/{userId}/ban` - Блокировка пользователя
- `PATCH /api/admin/users/{userId}/unban` - Разблокировка пользователя
- `PATCH /api/admin/users/{userId}/updateRole` - Изменение роли

**Возможности:**
- ✅ Просмотр всех пользователей в виде таблицы
- ✅ Фильтрация по статусу блокировки и роли
- ✅ Блокировка/разблокировка пользователя
- ✅ Изменение роли (USER ↔ ADMIN)
- ✅ Просмотр детальной информации о пользователе
- ✅ Пагинация

**Отображаемая информация:**
- ID пользователя
- Логин
- Email
- Роль (USER/ADMIN)
- Статус (Активен/Заблокирован)
- Имя и фамилия
- Телефон

### 5. Документы (Documents Management)

**API Endpoints:**
- `GET /api/admin/documents` - Получение списка документов
- `PATCH /api/admin/documents/{documentId}/verify` - Проверка документа

**Возможности:**
- ✅ Просмотр всех документов пользователей
- ✅ Фильтр "Только непроверенные документы" (включен по умолчанию)
- ✅ Подтверждение документа
- ✅ Просмотр информации о документе:
  - Тип документа
  - Серия и номер
  - Дата выдачи
  - Орган выдачи
  - Статус проверки

**Особенности:**
- Карточный вид для удобства просмотра
- Визуальное выделение непроверенных документов
- Быстрая проверка одним кликом

### 6. Справочники (Filters Management)

**API Endpoints:**
- `GET /api/admin/filters/brands` - Получение брендов
- `POST /api/admin/filters/brands` - Создание бренда
- `GET /api/admin/filters/models` - Получение названий моделей
- `POST /api/admin/filters/models` - Создание названия модели
- `GET /api/admin/filters/classes` - Получение классов
- `POST /api/admin/filters/classes` - Создание класса

**Возможности:**
- ✅ Управление справочником брендов
- ✅ Управление справочником названий моделей
- ✅ Управление справочником классов автомобилей
- ✅ Добавление новых значений через модальные окна
- ✅ Просмотр всех значений в каждом справочнике

**Три колонки справочников:**
1. **Марки** - бренды автомобилей (Toyota, BMW, Mercedes и т.д.)
2. **Названия моделей** - модели (Camry, X5, E-Class и т.д.)
3. **Классы** - классы автомобилей (Economy, Business, Premium и т.д.)

## Маршрутизация

```javascript
// Главная админ-панель
/admin

// Управление автомобилями
/admin/cars/new           # Создание автомобиля
/admin/cars/:id           # Детали/редактирование автомобиля

// Управление пользователями
/admin/users/:id          # Детали пользователя

// Управление контрактами
/admin/contracts/:id      # Детали контракта
```

## Защита маршрутов

Все админские маршруты защищены двумя уровнями:

1. **Аутентификация** - пользователь должен быть авторизован
2. **Авторизация** - пользователь должен иметь роль ADMIN

```javascript
meta: { 
  requiresAuth: true,    // Требуется авторизация
  requiresAdmin: true    // Требуется роль администратора
}
```

## UI/UX особенности

### Дизайн
- Современный интерфейс с градиентами
- Адаптивная верстка для мобильных устройств
- Карточный дизайн для удобства восприятия
- Иконки для визуальной навигации
- Цветовое кодирование статусов

### Интерактивность
- Hover эффекты на карточках
- Модальные окна для форм
- Подтверждающие диалоги для критических операций
- Индикаторы загрузки
- Плавные анимации и переходы

### Удобство использования
- Множественные фильтры для быстрого поиска
- Пагинация для больших списков
- Автодополнение в фильтрах
- Быстрые действия прямо из списков
- Breadcrumbs (кнопка "Назад")

## Обработка ошибок

Все компоненты используют централизованный обработчик ошибок:

```javascript
import { getErrorMessage } from '../../utils/errorHandler'

try {
  // API запрос
} catch (error) {
  alert(getErrorMessage(error, 'Ошибка по умолчанию'))
}
```

## Статусы и их отображение

### Статусы автомобилей
- 🟢 AVAILABLE - Доступен (зеленый)
- ⚪ UNAVAILABLE - Недоступен (серый)
- 🔵 IN_USE - В аренде (синий)
- 🟡 MAINTENANCE - Обслуживание (желтый)
- 🟣 RESERVED - Зарезервирован (фиолетовый)
- 🔴 CLOSED - Закрыт (красный)

### Статусы контрактов
- 🟢 ACTIVE - Активный (зеленый)
- 🟡 PENDING - В ожидании (желтый)
- 🔵 COMPLETED - Завершен (синий)
- 🔴 CANCELLED - Отменен (красный)
- 🟠 CANCELLATION_REQUESTED - Запрошена отмена (оранжевый)

### Роли пользователей
- 🔵 USER - Пользователь (синий)
- 🟣 ADMIN - Администратор (фиолетовый)

## Интеграция с API

Все компоненты используют централизованный API клиент:

```javascript
import api from '../../services/api'

// Примеры использования
const response = await api.get('/admin/cars')
await api.post('/admin/cars', carData)
await api.patch(`/admin/cars/${id}`, updateData)
await api.delete(`/admin/cars/${id}`)
```

## Пагинация

Стандартные параметры пагинации:

```javascript
const params = {
  page: 0,        // Номер страницы (начинается с 0)
  size: 20,       // Количество элементов на странице
  sort: 'field'   // Поле для сортировки (опционально)
}
```

## Валидация

Формы включают валидацию:
- Обязательные поля помечены звездочкой (*)
- HTML5 валидация для email, номеров и т.д.
- Минимальная/максимальная длина для строк
- Диапазоны для числовых полей

## Доступность

- Семантический HTML
- ARIA атрибуты для интерактивных элементов
- Keyboard navigation
- Focus states для всех интерактивных элементов

## Производительность

- Ленивая загрузка компонентов через `import()`
- Пагинация для уменьшения объема данных
- Debounce для поисковых запросов
- Оптимизированные изображения

## Расширяемость

Архитектура позволяет легко добавлять новые функции:
1. Создать новый компонент в `components/admin/`
2. Добавить вкладку в `AdminView.vue`
3. Зарегистрировать маршруты в `router/index.js`
4. Использовать существующие UI компоненты

## Итоги

Админ-панель полностью реализует спецификацию Swagger API и предоставляет:

✅ **6 основных модулей управления**
✅ **15+ API эндпоинтов**
✅ **Полный CRUD для всех сущностей**
✅ **Продвинутая фильтрация и поиск**
✅ **Управление статусами и ролями**
✅ **Работа с документами**
✅ **Современный и удобный интерфейс**
✅ **Адаптивный дизайн**
✅ **Защита маршрутов**
✅ **Обработка ошибок**

Все компоненты готовы к использованию и полностью интегрированы с backend API.
