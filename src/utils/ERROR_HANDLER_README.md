# Утилита обработки ошибок

## Описание

Универсальный обработчик ошибок для автоматического извлечения сообщений от бэкенда.

## Использование

### Основная функция: `getErrorMessage()`

```javascript
import { getErrorMessage } from '@/utils/errorHandler'

try {
  await someApiCall()
} catch (error) {
  // Автоматически извлекает сообщение от бэкенда
  const message = getErrorMessage(error, 'Сообщение по умолчанию')
  showNotification(message)
}
```

### Что делает функция?

1. **Извлекает сообщение от бэкенда** из разных полей:
   - `error.response.data.message`
   - `error.response.data.error`
   - `error.response.data.details`
   - `error.response.statusText`

2. **Обрабатывает сетевые ошибки**:
   - Если нет соединения с сервером → "Ошибка соединения с сервером"

3. **Возвращает fallback**:
   - Если ничего не найдено → использует `defaultMessage`

### Вспомогательные функции

```javascript
import { 
  isAuthError,        // Проверка статуса 401
  isValidationError,  // Проверка статусов 400/403
  isNotFoundError     // Проверка статуса 404
} from '@/utils/errorHandler'

try {
  await updateProfile(data)
} catch (error) {
  if (isAuthError(error)) {
    // Перенаправить на страницу входа
    router.push('/auth')
  } else {
    showMessage(getErrorMessage(error))
  }
}
```

## Примеры

### До (плохо - куча if-ов):

```javascript
catch (error) {
  if (error.response?.status === 403) {
    const errorMessage = error.response?.data?.message || 
                        error.response?.data?.error || 
                        'Ошибка валидации'
    showMessage(errorMessage, true)
  } else if (error.response?.status === 400) {
    const errorMessage = error.response?.data?.message || 
                        'Неверные данные'
    showMessage(errorMessage, true)
  } else if (error.response?.status === 401) {
    showMessage('Требуется авторизация', true)
  } else {
    showMessage('Ошибка при выполнении операции', true)
  }
}
```

### После (хорошо - одна строка):

```javascript
catch (error) {
  showMessage(getErrorMessage(error, 'Ошибка при выполнении операции'), true)
}
```

## Преимущества

✅ **DRY (Don't Repeat Yourself)** - код не дублируется  
✅ **Единая точка изменений** - правки в одном месте  
✅ **Автоматическая обработка** - работает для любых статусов  
✅ **Гибкость** - легко добавить новые поля для извлечения сообщений  
✅ **Читаемость** - код стал проще и понятнее  

## Расширение

Если нужно добавить извлечение из нового поля:

```javascript
export const getErrorMessage = (error, defaultMessage = 'Произошла ошибка') => {
  if (error.response) {
    const message = 
      error.response.data?.message ||
      error.response.data?.error ||
      error.response.data?.details ||
      error.response.data?.errorMessage ||  // Добавили новое поле
      error.response.statusText
    
    return message || defaultMessage
  }
  // ...
}
```

## Интеграция с бэкендом

Утилита работает с любыми стандартными форматами ошибок:

```json
// Вариант 1
{
  "message": "Данный телефон закреплен за другим аккаунтом"
}

// Вариант 2
{
  "error": "ValidationException",
  "message": "Данный телефон закреплен за другим аккаунтом"
}

// Вариант 3
{
  "details": "Данный телефон закреплен за другим аккаунтом",
  "code": "PHONE_ALREADY_EXISTS"
}
```

Все варианты автоматически обрабатываются! 🎉
